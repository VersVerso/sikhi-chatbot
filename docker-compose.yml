version: "3.9"
services:
  qdrant:
    # Pinned for predictable local/dev behavior across environments.
    image: qdrant/qdrant:v1.13.4
    ports:
      - "6333:6333"
    volumes:
      - qdrant_storage:/qdrant/storage

volumes:
  qdrant_storage:
